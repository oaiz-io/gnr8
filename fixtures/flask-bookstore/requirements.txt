# Fixture-local documentation of the framework this service targets.
#
# NOTE (Phase 1): DOCUMENTATION ONLY. Never pip-installed, never linked into
# gnr8-core. This phase ships STATIC source + red-by-design snapshots; no
# extractor runs against this fixture yet (pyextract lands in Phase 2). gnr8 will
# later derive API facts from Python's OWN type system (annotations, @dataclass
# field types) — NEVER from a runtime schema export or a third-party schema tool
# (AGENTS.md rule 1).
flask
